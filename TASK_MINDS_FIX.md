# TASK_MINDS_FIX — แก้ชั้นผู้มีจิตใจ (tiandao/mind) ตามผลรันจริง

> ใบงานสำหรับผู้ช่วยเขียนโค้ด (Qwen / Pi Coding Agent) — อ่านให้จบก่อนเริ่ม

## เป้าหมาย

ทำให้ `python -m unittest test_minds_fix -v` ผ่านทุกข้อ **โดยที่** `python -m unittest test_minds -v` และ `python test_determinism.py` ยังผ่านเหมือนเดิม

เทสต์ใน `test_minds_fix.py` คือสเปก ถ้าใบงานนี้กับเทสต์ขัดกัน ให้ยึดเทสต์

## กติกา (สำคัญ)

1. **แก้ได้เฉพาะ 5 ไฟล์นี้:**
   `tiandao/mind/config.py`, `tiandao/mind/manager.py`, `tiandao/mind/prompt.py`, `tiandao/mind/storyteller.py`, `tiandao/mind/runner.py`
2. **ห้ามแก้** `test_minds.py`, `test_minds_fix.py`, `tiandao/sim.py` และไฟล์อื่นนอก `tiandao/mind/`
3. แก้แบบ **เพิ่ม/เปลี่ยนเฉพาะจุด** ห้ามเขียนไฟล์ใหม่ทั้งไฟล์ ห้ามลบคอมเมนต์หรือโค้ดเดิมที่ไม่เกี่ยว
4. **ห้ามใช้ `random` หรือ `sim.rng`** ในโค้ดใหม่ (โลกต้องเดินซ้ำได้เหมือนเดิม — `test_determinism.py`)
5. คอมเมนต์ใหม่เขียนภาษาไทย อธิบายว่า "ทำไม" สั้นๆ
6. **ห้ามรันคำสั่ง git ที่เปลี่ยนสถานะ** (commit/checkout/stash/reset) — Claude จะตรวจงานโดยเทียบไฟล์เอง

## วิธีรันเทสต์ (จากโฟลเดอร์ `D:\Sim Dao`)

```powershell
python -m unittest test_minds_fix -v      # เป้าหมาย
python -m unittest test_minds -v          # ต้องยังผ่าน
python test_determinism.py                # ต้องยังผ่าน
```

รันทีละคลาสได้ เช่น `python -m unittest test_minds_fix.TestFix1Collapse -v`

---

## งานที่ 1 — มารบุกท่วมบันทึก และฆาตกรจากการบุกกลายเป็นตัวเอก

**ปัญหาที่เจอจากรันจริง:** 487 จาก 917 บรรทัดในบันทึกเป็น "มารบุก" เพราะเมื่อมารจับผู้มีจิตใจกิน มารตัวนั้นถูกรับเป็น "ผู้สืบเรื่อง" แล้วก็ไปกินคนต่อ วนเป็นลูกโซ่

### 1.1 ผู้สืบเรื่องที่เป็นฆาตกร — `config.py`, `manager.py`
- เพิ่มใน `config.py`:
  ```python
  SUCCESSOR_KILLER_KINDS = frozenset({"ประลอง", "ล้างแค้น", "ชิงสมบัติ", "ทรยศ", "หักหลัง", "ลอบสังหาร",
                                      "ดักปล้น", "จับกุมอาชญากร", "สงครามสำนัก", "ล่ามนุษย์มาร"})
  ```
- ใน `MindManager._mark_dead()` บรรทัดที่หา `killer` — ให้นับเป็นผู้สืบเรื่องได้ **เฉพาะเมื่อ `ev.kind` อยู่ใน `MC.SUCCESSOR_KILLER_KINDS`** (การต่อสู้ที่มีเจตนา) ส่วนศิษย์/ลูก/คู่ครอง/คนผูกพันยังเหมือนเดิม
- เทสต์: `TestFix1Successors`

### 1.2 เหตุการณ์ซ้ำ — `config.py`, `manager.py`
- เพิ่มใน `config.py`: `COLLAPSE_KINDS = frozenset({"มารบุก"})`
- ใน `MindManager.on_event()` ส่วนที่ผู้มีจิตใจเป็น **ผู้กระทำ แต่ไม่ได้เลือกเอง** (สาขา `elif in_a and self.minds[a].alive:`):
  ถ้า `ev.kind` อยู่ใน `MC.COLLAPSE_KINDS` ให้เขียนบันทึก (`_journal`) และความทรงจำ (`remember`) **แค่ครั้งแรกของ (cid, ปี = ev.day // 365, kind)** ครั้งถัดไปในปีเดียวกันให้แค่นับจำนวนแล้วข้าม
  - เก็บตัวนับไว้บน manager เช่น `self._collapsed = {}` คีย์ `(cid, year, kind)`
  - **อย่าลืม** `__setstate__` ต้อง `setdefault` ค่านี้ (เซฟเก่าโหลดได้)
  - ถ้าผู้กระทำตายในเหตุการณ์ที่ถูกข้าม ยังต้องเรียก `_mark_dead` เหมือนเดิม
  - เหตุการณ์ชนิดอื่นต้องบันทึกทุกครั้งเหมือนเดิม
- เทสต์: `TestFix1Collapse`

---

## งานที่ 2 — ผู้มีจิตใจไม่เคยได้เจอกัน

**ปัญหาที่เจอจากรันจริง:** 9 คนกระจายทั่วโลก ไม่มีการตัดสินใจไหนเลยที่มีเป้าหมายเป็นผู้มีจิตใจด้วยกัน (0 ครั้ง)

### 2.1 บ้านเกิดร่วม — `config.py`, `manager.py`
- เพิ่มใน `config.py`: `HOME_RADIUS_DAYS = 20`
- เพิ่ม attribute `self.home_place = -1` ใน `MindManager.__init__` และ `setdefault("home_place", -1)` ใน `__setstate__`
- ใน `ensure_cast()` ถ้า `home_place < 0` ให้เลือกบ้านครั้งเดียว:
  สถานที่ในโลก wid 0 ที่มี **จำนวนผู้มีสิทธิ์ (`_eligible`) อยู่ภายใน `HOME_RADIUS_DAYS` วันเดินทางมากที่สุด**
  - ระยะวันใช้ `travel.shortest_path_days(home, place, 0)` (ที่เดียวกัน = 0 วัน, คืน `None` = ไปไม่ถึง)
  - เลือกแบบไม่สุ่ม: เสมอกันให้เอาดัชนีสถานที่ที่น้อยกว่า
- ตอนคัดคน (ลูป `for c in ordered:`) ให้ **คนที่อยู่ใกล้บ้านมาก่อน** คนที่ไกล โดยยังเคารพเพดานเดิม (อาร์คีไทป์/ต่อสถานที่/สัดส่วนชาวบ้าน)
- เทสต์: `TestFix2Home`, `TestFix2Home2` (อย่างน้อย 75% ของผู้มีจิตใจต้องอยู่ในรัศมี และ `home_place` ต้องอยู่รอดหลังเซฟ/โหลด)

### 2.2 คนรอบตัว — `prompt.py`
- ใน `pick_people()` ผู้มีจิตใจที่ยังมีชีวิต (`sim.mind.minds[cid].alive`) ต้องถูกจัด **อยู่ก่อนทุกคน** (ก่อนศัตรู/มิตรด้วย) — ใช้ `getattr(sim, "mind", None)` เผื่อโลกไม่มีชั้นจิตใจ
- เทสต์: `TestFix2People.test_other_minds_listed_first`

### 2.3 จุดหมายเดินทาง — `prompt.py`
- ใน `pick_destinations()` สถานที่ที่มี **ผู้มีจิตใจคนอื่นที่ยังมีชีวิต อยู่ในโลกเดียวกัน** ต้องมาก่อน (เรียงตามระยะ) แล้วค่อยเป็นที่ที่มีวัตถุดิบที่ขาด/ข่าวลือ แล้วค่อยเป็นระยะทางปกติ
- หมายเหตุ (`note`) ของสถานที่นั้นต้องมีข้อความที่ **มีชื่อผู้มีจิตใจที่อยู่ที่นั่น** เช่น `"มี หลินซวี่ อยู่ที่นั่น"`
- ข้ามตัวเอง และข้ามสถานที่ที่ตัวเองยืนอยู่ (ไม่อยู่ใน `options` อยู่แล้ว)
- เทสต์: `TestFix2People.test_destinations_show_where_other_minds_are`

---

## งานที่ 3 — เรื่องเล่าเลยผลจริง / คำสั่งหลุด / ใช้ "ฉัน"

**ปัญหาที่เจอจากรันจริง:** ผลจริงคือ "ออกเดินทาง" แต่เรื่องเล่าให้ไปถึงและกลับมาแล้ว, มีประโยค "เขาใช้ความคิดในใจแทนบทสนทนา" หลุดเข้าเนื้อเรื่อง, บางเรื่องใช้ "ฉัน"

### 3.1 พรอมต์ — `storyteller.py` ฟังก์ชัน `build()`
ข้อความ user ต้อง:
- มีวลี `ห้ามเล่าเหตุการณ์ที่เกิดหลังจากผลนี้` **เสมอ**
- มีวลี `ข้า/เจ้า` เสมอ (สั่งให้ใช้สรรพนามนี้ทั้งบรรยายและบทพูด)
- ถ้า `entry["outcome"] == "ออกเดินทาง"` ต้องมีวลี `ยังไม่ถึงที่หมาย` (และ **ไม่มี** วลีนี้ถ้า outcome อื่น)
- **ต้องไม่มี** วลี `ใช้ความคิดในใจแทนบทสนทนา` (เปลี่ยนเป็นคำสั่งแบบอื่น เช่น "เล่าความรู้สึกผ่านการกระทำ")

### 3.2 ทำความสะอาดเรื่องเล่า — `config.py`, `storyteller.py`, `runner.py`
- เพิ่มใน `config.py`:
  ```python
  STORY_LEAK_PHRASES = ("ใช้ความคิดในใจแทนบทสนทนา", "[ให้เขียน]", "ฉากสั้นยาว 3-6 ย่อหน้า",
                        "จบที่ผลลัพธ์", "ไม่ต้องสรุปบทเรียน")
  ```
- เพิ่มฟังก์ชัน `clean_story(text: str) -> str` ใน `storyteller.py`:
  - ลบทุกวลีใน `STORY_LEAK_PHRASES` ออก (ลบเฉพาะวลี ส่วนอื่นของบรรทัดต้องอยู่)
  - เปลี่ยน `ฉัน` เป็น `ข้า` **ยกเว้น** เมื่อตามด้วย `ท` (คำอย่าง ฉันทะ/ฉันท์ ต้องไม่เปลี่ยน) — ใช้ regex `ฉัน(?!ท)`
  - เก็บช่องว่างซ้ำให้เหลือช่องเดียว และทิ้งบรรทัดว่าง
- ใน `MindRunner._write_story()` ให้เรียก `clean_story()` กับข้อความจากโมเดล **ก่อน** เขียนลงบันทึก
- เทสต์: `TestFix3Story`, `TestFix3Runner`

---

## เสร็จแล้วส่งอะไร

1. ผลรันเทสต์ทั้ง 3 คำสั่ง (คัดลอกบรรทัดสรุปท้ายมา)
2. รายชื่อไฟล์ที่แก้ และสรุปสั้นๆ ว่าแต่ละงานแก้ตรงไหน
3. ถ้ามีข้อไหนทำไม่ผ่าน ให้บอกตรงๆ ว่าติดอะไร — **ห้ามแก้เทสต์ให้ผ่านเอง**
