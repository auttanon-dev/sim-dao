# บันทึกผู้มีจิตใจ (tiandao/mind)

ระบบนี้ให้ตัวละครราว 40 คนในโลก **คิดเอง ตั้งเป้าหมายเอง วางแผนเอง และตัดสินใจเอง** แล้วบันทึกทุกครั้งเป็น "บันทึกชีวิต" ที่อ่านได้ว่า *คนนี้เป็นคนแบบไหน คิดอะไรอยู่ จึงเลือกทำแบบนั้น แล้วโลกตอบกลับมาอย่างไร*

แนวคิดได้มาจากเกมจำลองโลกบำเพ็ญเซียนที่ให้ตัวละครคิดเอง แต่เราไม่ได้คัดลอกโค้ดของเขามา ทั้งหมดเขียนขึ้นใหม่ให้เข้ากับเอนจินวิถีสวรรค์ของเราเอง กฎโลก ผังสถานที่ที่ต่อขยายตามแดนสาขา วิชา ตระกูล และการตัดสินผลทั้งหมดยังเป็นของ `tiandao/` เดิม

## เปิดใช้

```powershell
.\Start-Minds.ps1                          # อ่านได้ที่ http://127.0.0.1:8002 (ใช้ Ollama ตาม config_ai.py)
.\Start-Minds.ps1 -Paused                  # เปิดหน้าอ่านโดยยังไม่เดินโลก
.\Start-Minds.ps1 -Count 50 -Seed 7        # ผู้มีจิตใจ 50 คน ในโลกที่สร้างจาก seed 7
.\Start-Minds.ps1 -Provider lmstudio -Model 'google/gemma-4-e4b'
.\Start-Minds.ps1 -StoryModel 'hf.co/nectec/pathumma-thaillm-8b-think-3.0.0-GGUF:Q4_K_M'   # ใช้โมเดลอื่นเล่าเรื่อง
.\Start-Minds.ps1 -Provider mock           # ทดสอบระบบโดยไม่ใช้โมเดล (ความคิดเป็นข้อความแม่แบบ)
.\Start-Minds.ps1 -NoStory                 # ไม่แต่งเรื่องสั้น เร็วขึ้น
```

จากเทอร์มินัล (พิมพ์บันทึกออกมาให้อ่านสด):

```powershell
python run_minds.py --probe                # ส่งพรอมต์จริงหนึ่งครั้ง ดูว่าโมเดลตอบแล้วอ่านเป็นแผนได้ไหม ← ลองอันนี้ก่อน
python run_minds.py --years 3
python run_minds.py --provider mock --years 20
```

โลกและบันทึกอยู่ที่ `out/minds/`
- `world.save` คือโลกของระบบนี้ แยกจากโลกของโรงนิยายและท่องยุทธจักร จึงไม่เขียนทับกัน
- `journal.jsonl` คือบันทึกชีวิตทั้งหมด
- ลบโฟลเดอร์ `out/minds/` ทิ้งแล้วเปิดใหม่ = เริ่มโลกใหม่
- ถ้าต้องการเริ่มจากโลกเดิม ใช้ `-FromSave tiandao\world.save` (มีผลเฉพาะตอนที่ยังไม่มีโลกใน `out/minds/`)

## ทำงานอย่างไร

```
เอนจินเดิมคำนวณ "สิ่งที่ทำได้" (intent.weigh: อาชีพ นิสัย ร่างกาย สถานการณ์)
   │
   ├─ ตัวละครทั่วไป (~1,000 คน) → สุ่มตามน้ำหนักเหมือนเดิม เป็นฉากหลังของโลก
   │
   └─ ผู้มีจิตใจ (40 คน) → tiandao/mind
        1. ยังมีแผนค้างและทำต่อได้ → ทำขั้นถัดไปของแผน (ไม่ต้องเรียกโมเดล)
        2. แผนหมด / ทำต่อไม่ได้ / มีคนมาทำอะไรเรา / ชีวิตเปลี่ยนครั้งใหญ่ → คิดใหม่
           โมเดลเห็น: ตัวตน นิสัย เป้าหมายชีวิต ความทรงจำ เรื่องที่เพิ่งเกิดกับตัวเอง
                     คนรอบตัวและความสัมพันธ์ ที่ที่ไปได้ และรายการสิ่งที่ทำได้
           โมเดลตอบ: ความคิด อารมณ์ เป้าหมาย แผน 1-3 ขั้น (การกระทำ + คน + ที่หมาย + เหตุผล)
        3. เอนจินเดิมตัดสินผลตามกฎโลก — โมเดลเลือกเจตนาได้ แต่ไม่เคยเลือกผล
        4. บันทึก: ความคิด → การตัดสินใจ → ผลจริง (+ เรื่องสั้น ถ้าเป็นเหตุการณ์สำคัญ)
```

**ทำไมจึงเลือกออกแบบแบบนี้**

- **ให้โมเดลเลือกจากเมนู ไม่ให้แต่งการกระทำขึ้นเอง:** ถ้าปล่อยให้แต่งเอง โมเดลจะทำสิ่งที่กฎโลกไม่มี และเอนจินก็ตัดสินผลให้ไม่ได้ เมนูจึงมาจากตาราง `EVENT_TABLE` ส่วนการเลือกว่าจะทำอะไร กับใคร และที่ไหน เป็นอิสระของตัวละครทั้งหมด
- **วางแผนหลายขั้น:** ตัวละครทำอะไรต่อเนื่องเป็นเรื่องเดียวกันได้ เช่น ไปหาแร่ แล้วหลอมอาวุธ แล้วท้าประลอง ไม่เปลี่ยนใจทุกเทิร์น ข้อดีอีกข้อคือเรียกโมเดลน้อยลงราว 40%
- **ใช้รหัส P1/D2 แทนชื่อคนและสถานที่:** ในโลกมีคนชื่อซ้ำกันจริง และโมเดลเล็กมักสะกดชื่อผิด
- **คัดตัวละครจากโลกมนุษย์ก่อน:** ผู้มีจิตใจจะอยู่ใกล้กันและได้พบกันจริง มีการกันที่ไว้ให้ชาวบ้านธรรมดาราว 1 ใน 5 ตามกฎ "ชาวบ้านก็ตรัสรู้วิถีของตนได้"
- **เมื่อผู้มีจิตใจตาย:** คนที่สืบเรื่องต่อจะตื่นรู้แทน เรียงตามลำดับ ผู้ที่ฆ่า → ศิษย์ → ลูก → คู่ครอง
- **ถ้าโมเดลติดต่อไม่ได้หรือตอบพัง:** ตัวละครใช้ "สัญชาตญาณ" (น้ำหนักเดิม) แทน โลกไม่หยุด และบันทึกเหตุผลไว้ให้ดู
- **ภัยธรรมชาติยังเป็นชะตาฟ้า:** ไม่มีใครเลือกเอง จึงยังเกิดตามโอกาสเดิม

**ไม่มีผลกับระบบเดิม:** ถ้าไม่ได้เปิดระบบนี้ ทุกจุดที่แก้ใน `sim.py` จะทำงานแบบเดิมทุกประการ `test_determinism.py` ยังผ่าน และเทียบ log 30,000 เหตุการณ์กับเอนจินก่อนแก้ได้ผลตรงกันทุกบรรทัด

## หน้าอ่าน

- **แถบควบคุม:** กำหนดเวลาที่จะเดินโลกเป็นนาทีและจำนวนผู้มีจิตใจได้ก่อนกดเริ่ม
- **แต่งนิยาย:** เลือกตัวละครจากรายชื่อ กำหนดจำนวนบท แล้วกด `แต่งนิยายจากชีวิตคนนี้`
  งานเขียนทำเบื้องหลัง บันทึกทุกบท และดาวน์โหลด Markdown ได้จากหน้าเดียวกัน

- **ซ้าย:** รายชื่อผู้มีจิตใจ แต่ละคนแสดงอารมณ์ ขั้นพลัง และสิ่งที่ตั้งใจทำอยู่ ผู้ล่วงลับแยกไว้ด้านล่าง
- **กลาง:** ความเคลื่อนไหวทั้งหมด หรือบันทึกของคนที่เลือก
  - การตัดสินใจแต่ละครั้งแสดงความคิด การกระทำ เหตุผล และผลจริง
  - ป้ายบอกที่มาของการกระทำ: `คิดใหม่` / `ตามแผน` / `สัญชาตญาณ` / `ชะตาฟ้า`
  - สิ่งที่คนอื่นทำกับตัวละคร การตื่นรู้ และความตาย
  - ปุ่ม "อ่านเป็นเรื่องเล่า"
- **โปรไฟล์:** ตัวตน เป้าหมายชีวิต แผนที่ค้าง คนสำคัญ ความรู้สึกต่อผู้อื่น และดาวน์โหลดชีวประวัติเป็น Markdown

## ปรับแต่ง (`tiandao/mind/config.py` หรือตัวแปรแวดล้อม)

| ค่า | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `TIANDAO_MIND_COUNT` | 40 | จำนวนผู้มีจิตใจ |
| `TIANDAO_MIND_PROVIDER` | ollama | ollama / lmstudio / mock |
| `TIANDAO_MIND_MODEL` | `config_ai.OLLAMA_MODEL` (Typhoon2.5 4B) | โมเดลที่ใช้คิด |
| `TIANDAO_MIND_STORY_MODEL` | ตัวเดียวกับที่ใช้คิด | โมเดลเล่าเรื่อง |
| `TIANDAO_MIND_STORY` | 1 | 0 = ไม่แต่งเรื่องสั้น |
| `PLAN_MAX_STEPS` | 3 | วางแผนล่วงหน้ากี่ขั้น |
| `STORY_P` | ตามหมวด | โอกาสแต่งเรื่องสั้นหลังเหตุการณ์แต่ละแบบ |

**ประมาณเวลา (ยังไม่ได้วัดบนเครื่องจริง)**

- จากการรันทดสอบด้วย mock พบว่า 40 คน คิดใหม่ราว 17 ครั้งต่อปีจำลอง และเล่าเรื่องราว 7 ครั้งต่อปี
- ถ้าโมเดล 4B ใช้เวลาคิดครั้งละ 5-10 วินาที จะได้ราว 3-5 นาทีต่อปีจำลอง
- โลกเดินช้าเร็วตามจังหวะของเหตุการณ์ เช่น บำเพ็ญหนึ่งครั้งกินเวลาหลายร้อยวัน

## ไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `tiandao/mind/manager.py` | Mind/MindManager: คัดคน ตัดสินใจ แผน ความทรงจำ ความตาย ผู้สืบเรื่อง บันทึก |
| `tiandao/mind/prompt.py` | สร้างพรอมต์ และแกะคำตอบเป็นแผนที่เอนจินทำได้จริง |
| `tiandao/mind/persona.py` | แปลงตัวเลขของตัวละครเป็นคำพูด (ใช้ทั้งพรอมต์และหน้าอ่าน) |
| `tiandao/mind/actions.py` | เมนูการกระทำและคำอธิบาย |
| `tiandao/mind/backend.py` | ตัวเชื่อม Ollama / LM Studio / mock |
| `tiandao/mind/storyteller.py` | เล่าการตัดสินใจเป็นเรื่องสั้นหลังเหตุการณ์จบ |
| `tiandao/mind/runner.py` | ลูปเดินโลก เซฟ อ่านบันทึก |
| `mind_app.py`, `templates/minds.html`, `Start-Minds.ps1` | หน้าอ่าน |
| `run_minds.py` | ใช้งานจากเทอร์มินัล และทดสอบโมเดลด้วย `--probe` |
| `test_minds.py` | เทสต์ (ไม่ต้องเปิดโมเดล) |

จุดที่แก้ในไฟล์เดิม
- `tiandao/sim.py` 4 จุด: เลือกการกระทำ, ข้ามการสุ่มท้าชิงตำแหน่งสำนักของคนที่ตัดสินใจเองแล้ว, บันทึกหลังลงมือ, จุดหมายเดินทาง
- `tiandao/persist.py`: เติมค่าที่เซฟเก่าขาด (`branch_wids`, `lord_seal`, `lord_seals_made`)
  - เจอระหว่างทดสอบว่า `tiandao/world.save` ปัจจุบันล้มภายใน 8,000 เหตุการณ์ด้วย `AttributeError: branch_wids` ทั้งในเอนจินเดิมและเอนจินใหม่
  - ปัญหานี้กระทบโรงนิยายด้วย

ทดสอบ: `python -m unittest test_minds -v` · `python test_determinism.py`

## ยังไม่ได้ทำ (ต่อยอดได้)

- ยังไม่ได้วัดคุณภาพความคิดจากโมเดลจริง ควรรัน `python run_minds.py --probe` แล้วดูผลก่อน
- บทสนทนาระหว่างผู้มีจิตใจสองคนที่เจอกัน (ตอนนี้แต่ละคนคิดแยกกัน อีกฝ่ายรู้เรื่องผ่าน "เรื่องที่เพิ่งเกิดกับข้า")
- ส่งบันทึกชีวิตของคนที่ถูกใจเข้าโรงนิยายให้เขียนเป็นตอนยาว
- จุดตัวละครบนแผนที่ท่องยุทธจักร
