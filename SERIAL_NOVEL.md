# โลกที่เล่าเป็นนิยายต่อเนื่อง

เปิด `Start-Novel.ps1` แล้วอ่านที่ http://127.0.0.1:8001 ระบบใช้ Ollama และโมเดลภาษาไทยที่ติดตั้งไว้แล้ว ไม่ต้องเลือกตัวเอกหรือสั่งทีละตอน

## ใช้ Gemma 4 E4B บน LM Studio

1. โหลด Gemma 4 E4B ใน LM Studio แล้วเปิด Local Server ในแท็บ Developer
2. ตั้ง Context Length อย่างน้อย 8192 ตามงบฉากของระบบ หากขึ้นข้อความ context เต็มให้เพิ่มค่านี้ โมเดลต้องมีพื้นที่สำหรับทั้งคำสั่งและคำตอบ
3. เริ่มทดลองโดยปิด Enable Thinking เพื่อให้งบคำตอบใช้กับงานเขียน
4. เปิดโรงนิยายด้วยคำสั่ง PowerShell:

```powershell
.\Start-Novel.ps1 -Provider lmstudio -Model 'google/gemma-4-e4b'
```

ใช้ชื่อโมเดลตรงกับ identifier ที่ LM Studio แสดงจริง (ตรวจได้ด้วย `Invoke-RestMethod http://127.0.0.1:1234/v1/models`) ชื่ออาจต่างจากตัวอย่าง หากใช้พอร์ตอื่นระบุ `-BaseUrl 'http://127.0.0.1:1234/v1'` และถ้าเปิดการยืนยันตัวตน ให้ตั้งตัวแปร `TIANDAO_WRITER_API_KEY` ก่อนเปิดโปรแกรม

โหมด LM Studio ใช้โมเดลที่เลือกทั้งวางโครง บทสนทนา และร้อยแก้ว หากต้องการแยกโมเดลให้ใช้ `-StructureModel 'ชื่อโมเดล'` ทั้งสองชื่อจะถูกเรียกผ่าน LM Studio เครื่องเดียวกัน หน้าอ่านแสดงโมเดลที่กำลังใช้และเหตุผลเมื่อยังเชื่อมต่อไม่ได้ ระบบจะรอโดยไม่เดินโลกหรือข้ามฉาก และไม่สลับไปใช้โมเดลอื่นเอง

หากคำตอบถูกตัดเพราะเต็มขีดจำกัด token หรือ API ล้มเหลว ระบบจะพักพร้อมข้อความแก้ไข และยังไม่บันทึกเป็นตอน กด “เขียนต่อ” หลังแก้ปัญหาแล้วเพื่อทำฉากเดิมต่อ ตอนที่สำเร็จบันทึก provider และโมเดลทั้งสองบทบาทไว้ด้วย

กลับไปใช้ชุดเดิมได้ด้วย `.\Start-Novel.ps1 -Provider ollama` (ปิดเซิร์ฟเวอร์เดิมก่อนเปิดใหม่) หรือตั้งค่าผ่าน `TIANDAO_WRITER_PROVIDER`, `TIANDAO_WRITER_BASE_URL`, `TIANDAO_WRITER_MODEL`, `TIANDAO_WRITER_STRUCTURE_MODEL` เมื่อรัน `novel_app.py` ผ่าน uvicorn โดยตรง ตัวเลือกใน launcher คืนค่าตัวแปรที่เปลี่ยนเมื่อโปรแกรมปิด

การรองรับ API ยังไม่ใช่ผลประเมินสำนวน Gemma: ต้องทดลองอ่านฉากจริงเพื่อประเมินภาษาไทย บทสนทนา และความตรงกับเหตุการณ์ของ sim

## การเดินเรื่องและบันทึกตอน

โลกเดิมอยู่ที่ `tiandao/world.save` ระบบเลือกตัวเอกที่มีจุดเปลี่ยนอย่างน้อย 3 ครั้ง เล่าเรื่องของคนเดิมตามลำดับเหตุการณ์ เมื่อมีวัตถุดิบพอจะพักการเดินเวลาเพื่อเขียนตอน จากนั้นจึงสร้างเหตุการณ์เพิ่มตามที่ต้องการ ตัวละครตัดสินใจด้วย SimDao ส่วนโมเดลรับหน้าที่เรียบเรียงฉากและบทสนทนา

ตอนที่เสร็จอยู่ใน `out/serial/chapter-*.json` สารบัญและตัวเอกอยู่ใน `out/serial/series.json` บันทึกเหตุการณ์สำหรับนิยายชุดนี้อยู่ใน `out/serial/world.events.jsonl` แยกจากคลังเหตุการณ์เก่าที่อาจเป็นคนละโลกกับบันทึกปัจจุบัน ตอนที่ไม่ผ่านการตรวจจะอยู่ใน `draft.json` และยังไม่ลงสารบัญ ระบบลองแก้ได้ 3 ครั้งก่อนแสดงข้อผิดพลาดให้ตรวจสอบ

ปิดแล้วเปิดใหม่จะอ่านตอนที่บันทึกสำเร็จและเริ่มจากเหตุการณ์ถัดไป ไม่เขียนตอนเดิมซ้ำ ใช้ปุ่ม “พักระบบ” เพื่อพักทั้งวงจร หรือ `Start-Novel.ps1 -Paused` เพื่อเปิดอ่านโดยไม่เริ่มเขียนทันที การพักอาจต้องรอคำตอบที่กำลังสร้างอยู่ (หนึ่งคำขอมีเวลารอสูงสุด 300 วินาที)

การปิดแท็บไม่หยุดงาน ต้องพักผ่านหน้าอ่านหรือปิดเซิร์ฟเวอร์ นิยายที่เสร็จแล้วเก็บไว้เสมอ และดาวน์โหลดรวมเป็น Markdown ได้ อย่าเปิด daemon หรือแดชบอร์ดอีกตัวให้เขียนโลกไฟล์เดียวกันพร้อมกัน ระบบป้องกันการเปิดโรงนิยายซ้ำสำหรับโลกเดียวกันด้วย file lock

ก่อนเปิดอัตโนมัติจะสำรองโลกไว้ใน `tiandao/backups/` ชื่อ `world.save.before-novel-*` โมเดลที่ใช้ตามค่าเดิมใน `tiandao/ai/config_ai.py`: Pathumma สำหรับร้อยแก้ว และ Typhoon สำหรับโครง/บทสนทนา

คำบรรยายที่โมเดลสร้างยังควรประเมินด้านสำนวนและความต่อเนื่องจากการอ่านจริง ตัวตรวจอัตโนมัติช่วยคัดความยาว โครงฉาก บทสนทนา และข้อผิดพลาดบางชนิด ไม่ใช่การรับประกันวรรณศิลป์หรือความตรงข้อเท็จจริงทุกประโยค

ทดสอบหลัก: `python -m unittest test_serial test_writer_backend -v` และ `python test_writer.py`

## การแก้คำตอบขาดกลางประโยค (16 กันยายน 2026)

การลองฉากแรกจริงพบว่า `/no_think` ในข้อความอย่างเดียวไม่พอ: คำขอเดิมใช้ครบ 482 token แล้วจบด้วย `done_reason=length` และเหลือร้อยแก้วเพียง 383 ตัวอักษร จึงเพิ่ม `think: false` ผ่าน Ollama API สำหรับโมเดลที่ตั้งใจปิดโหมดคิด และเพิ่มงบเริ่มต้นของร้อยแก้วเป็นอย่างน้อย 1200 token

บาง template ของโมเดลที่นำเข้ายังส่งคำอธิบายภาษาอังกฤษพร้อม `</think>` โดยไม่มีแท็กเปิด ตัวกรองจึงรองรับรูปแบบนี้ด้วย และตัวตรวจฉากจะปฏิเสธข้อความภาษาอังกฤษที่หลุดเข้ามาเป็นเนื้อเรื่อง

ทั้ง Ollama ในโรงนิยายและ LM Studio ตรวจว่าคำตอบถูกตัดหรือไม่ ถ้าถูกตัดจะเขียนเฉพาะ pass เดิมใหม่ด้วยงบเพิ่มขึ้นสองเท่า สูงสุด 4096 token และตรวจคำสั่งพักระหว่างคำขอ ถ้ายังไม่จบจะพักพร้อมข้อผิดพลาด โดยไม่ลงสารบัญเป็นตอนสำเร็จ การขยายงบไม่ลดเกณฑ์ตรวจความยาวหรือความครบของบทพูด

ผลทดสอบการส่งคำขอจริงก่อนแก้ตัวกรองเก็บที่ `out/writer-budget-probe.json` (เป็นข้อมูลวินิจฉัย ไม่ใช่ตอนนิยายสำหรับอ่าน) ทดสอบตัวกรองเพิ่มเติมด้วย `python test_llm_agent_parsing.py`

## เปิดต่อจากงานที่เขียนค้างไว้ (17 กันยายน 2026)

โรงนิยายบันทึก `out/serial/work-in-progress.json` หลังวางโครงฉาก หลังร่างบทพูดแต่ละช่วง และหลังเขียนหรือซ่อมร้อยแก้วแต่ละช่วง ใช้การเขียนไฟล์ชั่วคราวแล้วแทนที่ไฟล์เดิม จึงยังมีสำเนาล่าสุดที่บันทึกเสร็จหากโปรแกรมหยุดกลางคำขอ

เมื่อกดเขียนต่อหรือเปิดระบบใหม่ จะนำงานที่บันทึกไว้กลับมาใช้และเรียกโมเดลเฉพาะส่วนที่เหลือ ถ้าทั้งตอนไม่ผ่านเพราะช่วงเดียว ระบบจะซ่อมช่วงนั้นโดยรักษาโครง บทพูด และร้อยแก้วส่วนอื่นไว้ งานที่ยังไม่ได้รับคำตอบหรือยังไม่ได้บันทึกต้องเรียกใหม่

งานค้างผูกกับข้อมูลฉาก บริบทตอนก่อนหน้า โมเดล และเซิร์ฟเวอร์ที่ใช้ หากข้อมูลเหล่านี้เปลี่ยนจะเริ่มร่างใหม่เพื่อไม่ผสมร่างคนละชุด ไฟล์ที่เสียจะทำให้ระบบพักและแจ้งชื่อไฟล์ให้ตรวจ โดยไม่เขียนทับร่างเสียเงียบๆ ร่างก่อนเพิ่มระบบนี้มีเพียงร้อยแก้วรวม จึงยังเปิดต่อรายช่วงไม่ได้

ทดสอบการปิดกลางฉากและเปิดต่อจากไฟล์จริงในโฟลเดอร์ชั่วคราว:
`python -m unittest test_serial test_writer_backend test_writer_checkpoint -v`
