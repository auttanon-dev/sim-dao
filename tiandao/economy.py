# -*- coding: utf-8 -*-
"""เศรษฐกิจของโลกนี้ — ปราณคือทรัพยากรเดียว เงินเป็นเพียงรูปของมัน

    from tiandao import economy as EC

แนวคิดหลักที่ทุกอย่างในไฟล์นี้ตั้งอยู่บน
==========================================
**หินวิญญาณคือปราณที่ถูกอัดเป็นก้อน** ไม่ใช่เหรียญที่มีค่าเพราะคนตกลงกันว่ามีค่า

นี่คือการตัดสินใจเชิงออกแบบที่สำคัญที่สุดในไฟล์นี้ และมันแก้ปัญหาที่ตามหลอกหลอนโลกนี้
มาตลอดได้ในทีเดียว: ก่อนหน้านี้ "เงิน" เป็นสสารอีกชนิดที่ลอยอยู่ข้างๆ ระบบพลัง มันจึงถูก
เสกขึ้นมาได้โดยไม่มีใครรู้ (org.monthly_resource = 10000 ทุกเดือนจากอากาศ กว่าจะจับได้
ก็ปีที่ 152 ตอนที่คนเดียวถือเงิน 60% ของทั้งโลกแล้ว) พอหินวิญญาณ **คือ** ปราณ
การเสกเงินก็กลายเป็นการเสกพลังงาน ซึ่งบัญชีคลังฟ้าจับได้ทันทีในปีแรก

ผลที่ตามมาโดยไม่ต้องเขียนกฎเพิ่ม
  · "เงินมากน้อยขึ้นกับทรัพยากรที่หาได้" เป็นจริงตามนิยาม ไม่ใช่ตามความตั้งใจ
  · ขุดหินวิญญาณ = ดูดปราณออกจากโลก โลกที่ร่ำรวยที่สุดคือโลกที่กำลังถูกดูดจนแห้ง
  · เผาหินวิญญาณเพื่อบำเพ็ญ = ปราณนั้นหายไปจากระบบจริงๆ ไม่ได้ย้ายมือ
  · เหรียญทองของปุถุชนยังอยู่ แต่มันเป็นคนละสกุล และแลกกันได้ตามปริมาณปราณเท่านั้น

สองสกุลเงิน
===========
    ch.money   — เหรียญทอง ของปุถุชน ใช้ซื้อข้าวปลา จ้างวาน เสียภาษี
    ch.stones  — หินวิญญาณ ของผู้ฝึก เก็บเป็น {เกรด: จำนวน} จำนวนเป็นทศนิยมได้
                 เพราะหินที่ถูกดูดไปครึ่งก้อนแล้วเป็นของปกติในยุทธภพ

เกรดของหินผูกกับ **ชั้นของแดนที่มันเกิด** ไม่ใช่ความบังเอิญ โลกชั้นสูงปราณหนาแน่นกว่า
หินที่ตกผลึกที่นั่นจึงอัดปราณได้มากกว่าต่อก้อน และเพราะมูลค่าผูกกับปริมาณปราณล้วนๆ
"ค่าตามคุณภาพ" จึงไม่ต้องมีตารางราคาแยก — มันเป็นตัวเลขเดียวกัน

ทำไมเป็นทวีคูณ (STONE_PER_GRADE) ไม่ใช่เชิงเส้น: ถ้าเชิงเส้น หินชั้นสูงจะเป็นแค่
"หินชั้นต่ำหลายก้อนมัดรวมกัน" ไม่มีเหตุผลให้ใครอยากขึ้นไปเอาถึงแดนบน พอเป็นทวีคูณ
หินหนึ่งก้อนจากแดนบนมีค่าเท่าทั้งกระเป๋าของคนแดนล่าง ซึ่งเป็นเหตุผลของการข้ามฟ้า
และของการที่คนแดนบนมองคนแดนล่างเป็นคนจน
"""
import math

from . import config as C
from . import physics as PHYS

GRADE_NAMES = ("หินวิญญาณชั้นต้น", "หินวิญญาณชั้นกลาง", "หินวิญญาณชั้นสูง",
               "หินวิญญาณชั้นเลิศ", "หินวิญญาณชั้นเทพ", "หินวิญญาณชั้นปฐม")


def grade_name(grade: int) -> str:
    g = max(0, min(len(GRADE_NAMES) - 1, int(grade)))
    return GRADE_NAMES[g]


def stone_qi(grade: int) -> float:
    """ปราณที่อัดอยู่ในหินหนึ่งก้อนของเกรดนี้ — และเป็นมูลค่าของมันด้วยในตัว"""
    g = max(0, min(len(GRADE_NAMES) - 1, int(grade)))
    return C.STONE_QI_BASE * (C.STONE_PER_GRADE ** g)


def stone_gold(grade: int) -> float:
    """ราคาเป็นเหรียญทองของปุถุชน — มีไว้ให้ตลาดที่คนสองพวกเจอกันเท่านั้น

    อัตราแลกไม่ใช่ตัวเลขที่ตั้งเอง แต่เป็น "ปราณหนึ่งหน่วยซื้อแรงงานปุถุชนได้เท่าไร"
    ปุถุชนไม่มีทางดูดปราณได้เอง หินวิญญาณกับเขาจึงมีค่าเท่าที่ขายต่อให้ผู้ฝึกได้
    ซึ่งแปลว่าผู้ฝึกเป็นคนกำหนดราคาเสมอ และปุถุชนเป็นฝ่ายรับ — ความเหลื่อมล้ำ
    ระหว่างสองชนชั้นในโลกนี้จึงเป็นเรื่องโครงสร้าง ไม่ใช่เรื่องใครขยันกว่าใคร
    """
    return stone_qi(grade) * C.STONE_GOLD_PER_QI


# ------------------------------------------------------------------ กระเป๋าหินวิญญาณ
def purse(ch) -> dict:
    p = getattr(ch, "stones", None)
    if not isinstance(p, dict):
        p = {}
        ch.stones = p
    return p


def add_stones(ch, grade: int, count: float) -> None:
    if count <= 0:
        return
    p = purse(ch)
    g = max(0, min(len(GRADE_NAMES) - 1, int(grade)))
    p[g] = p.get(g, 0.0) + float(count)


def purse_qi(ch) -> float:
    """ปราณทั้งหมดที่ถืออยู่ในรูปหิน = ความมั่งคั่งที่แท้จริงของผู้ฝึกคนหนึ่ง"""
    return sum(stone_qi(g) * n for g, n in purse(ch).items())


def burn_qi(ch, need: float) -> float:
    """เผาหินเพื่อเอาปราณออกมาใช้ — เริ่มจากก้อนเล็กก่อนเสมอ

    เริ่มจากเกรดต่ำสุดเพราะคนเก็บของมีค่าไว้ก้นถุงและใช้เศษก่อน เป็นพฤติกรรมที่เห็น
    ได้ทั้งในคนจริงและในเรื่องเล่า และทำให้หินชั้นสูงสะสมอยู่กับคนที่ไม่เคยต้องใช้มัน
    ซึ่งเป็นสัญญาณความมั่งคั่งที่อ่านออกจากกระเป๋าได้โดยไม่ต้องคำนวณ

    คืนปราณที่เอาออกมาได้จริง (อาจน้อยกว่าที่ขอ ถ้าของในกระเป๋าไม่พอ)
    """
    want = max(0.0, float(need))
    if want <= 0:
        return 0.0
    p = purse(ch)
    got = 0.0
    for g in sorted(p):
        if got >= want - 1e-12:
            break
        unit = stone_qi(g)
        have = p[g]
        if have <= 0:
            continue
        take_units = min(have, (want - got) / unit)
        p[g] = have - take_units
        got += take_units * unit
    for g in [g for g, n in p.items() if n <= 1e-9]:
        del p[g]
    return got


def mint(qi_amount: float, grade: int) -> float:
    """แปลงปราณดิบเป็นจำนวนหินของเกรดหนึ่ง — ใช้ตอนขุด ตอนตกผลึก ตอนแบ่งผลผลิตสำนัก"""
    unit = stone_qi(grade)
    return max(0.0, float(qi_amount)) / unit if unit > 0 else 0.0


# ------------------------------------------------------------------ หยุดฝึกไม่ได้
def upkeep_qi(realm: int) -> float:
    """ปราณที่ต้องใช้ต่อปีเพียงเพื่อ **คงขั้นเดิมไว้** ยังไม่นับการเลื่อนขั้น

        M(R) = c · (1+R)^b

    ของจริงที่ใกล้ที่สุดคือกฎของ Kleiber ที่อัตราเมแทบอลิซึมของสิ่งมีชีวิต ∝ มวล^(3/4)
    แต่ที่นี่ผมใช้ b > 1 โดยตั้งใจฝืนชีววิทยา และควรบันทึกเหตุผลไว้ให้ชัด:

    ถ้า b < 1 ขั้นที่สูงขึ้นจะ "ถูกลง" ต่อหนึ่งหน่วยพลัง แปลว่ายิ่งแข็งยิ่งเลี้ยงตัวง่าย
    เพดานของโลกก็จะไม่มีอยู่จริง ใครขึ้นไปได้ก็อยู่ได้ตลอดกาล กฎ "หยุดฝึกไม่ได้" จะไม่
    กดดันใครเลย พอ b > 1 ต้นทุนวิ่งหนีรายได้ ผู้ฝึกจึงถูกบังคับให้หาทรัพยากรเพิ่มตลอดเวลา
    หรือไม่ก็ไหลลง — ซึ่งคือกติกาที่ต้องการ
    """
    return C.UPKEEP_QI_BASE * ((1.0 + max(0, int(realm))) ** C.UPKEEP_QI_POW)


def supported_realm(qi_per_year: float) -> float:
    """ขั้นสูงสุดที่ปราณเท่านี้ต่อปีเลี้ยงไว้ได้ — กลับสมการ upkeep_qi ตรงๆ

        R* = (รายได้ / c)^(1/b) - 1

    สมการเดียวตอบสองคำถามพร้อมกัน ซึ่งเป็นเหตุผลที่เลือกใช้กฎกำลังตั้งแต่แรก
      · "คนนี้ขึ้นไปได้สูงสุดแค่ไหน"  — เพดานส่วนตัว
      · "ถ้าเขาหยุดหาของ จะไหลลงไปหยุดที่ไหน" — เป้าหมายของการถดถอย
    คนที่รายได้เป็นศูนย์ได้ R* = -1 ซึ่งต่ำกว่าปุถุชน แปลว่าไหลลงจนหมดสภาพผู้ฝึกจริงๆ
    """
    q = max(0.0, float(qi_per_year))
    if C.UPKEEP_QI_BASE <= 0:
        return float(C.REALM_CAP)
    return (q / C.UPKEEP_QI_BASE) ** (1.0 / C.UPKEEP_QI_POW) - 1.0


def absorb_per_year(realm: int, qi_density: float) -> float:
    """ปราณที่ดูดจากฟ้าดินได้เองต่อปี — **ไม่ต้องใช้เงินสักเหรียญ**

    นี่คือวาล์วนิรภัยของกฎ "หยุดฝึกไม่ได้" ถ้าการคงขั้นต้องจ่ายอย่างเดียว คนจนจะไหลลง
    จนหมดและโลกจะเหลือแต่คนรวย ซึ่งไม่ใช่ยุทธภพ — ยุทธภพมีคนยากไร้ที่นั่งบำเพ็ญ
    ในถ้ำร้างแล้วแข็งแกร่งขึ้นได้ เพราะ **ปราณฟ้าดินไม่ได้เป็นของใคร**

        A(R, ρ) = a · (ρ/ρ₀) · (1+R)^β        โดย β < b

    เลขชี้กำลังต้องต่ำกว่าของ upkeep (β < b) ถ้าเท่ากัน อัตราส่วนจะไม่ขึ้นกับขั้นเลย
    ที่หนึ่งจะเลี้ยงได้ทุกขั้นหรือไม่ได้สักขั้น ไม่มีตรงกลาง พอ β < b การเลื่อนขั้นจึงมี
    "ราคาที่ต้องจ่ายเพิ่ม" เสมอ และแต่ละสถานที่มีเพดานของมันเองที่คำนวณออกมาได้

    ผลที่ตามมาซึ่งเป็นหัวใจของกฎนี้: **ที่ยืนกำหนดว่าคุณค้างอยู่ได้สูงแค่ไหน**
      · แหล่งปราณธรรมดา เลี้ยงได้ราวขั้น 3
      · ภูเขาศักดิ์สิทธิ์ เลี้ยงได้ราวขั้น 6
      · ที่ที่ถูกขุดจนโทรม เลี้ยงไม่ได้แม้แต่ขั้น 1 — คนต้องย้ายหรือไหลลง
      · แดนชั้นบน เลี้ยงได้เกินเพดานของแดนล่างทั้งหมด ซึ่งคือเหตุผลของการข้ามฟ้า
    หินวิญญาณจึงไม่ใช่ทางเดียว มันคือ **ทางลัดสำหรับคนที่ไม่มีที่ยืนดีพอ**
    """
    rho = max(0.0, float(qi_density)) / max(1e-9, C.QI_REFERENCE)
    return C.ABSORB_BASE * rho * ((1.0 + max(0, int(realm))) ** C.ABSORB_POW)


def place_ceiling(qi_density: float) -> float:
    """ขั้นสูงสุดที่ "ที่ตรงนี้" เลี้ยงไว้ได้โดยไม่ต้องพึ่งหินวิญญาณเลย

    แก้ A(R,ρ) = M(R) หาค่า R ได้รูปปิด ไม่ต้องวนหา:
        a·ρ_rel·(1+R)^β = c·(1+R)^b   →   (1+R)^(b-β) = a·ρ_rel/c
    """
    rho = max(0.0, float(qi_density)) / max(1e-9, C.QI_REFERENCE)
    if rho <= 0 or C.UPKEEP_QI_BASE <= 0:
        return -1.0
    gap = C.UPKEEP_QI_POW - C.ABSORB_POW
    if gap <= 1e-9:
        return float(C.REALM_CAP)
    ratio = C.ABSORB_BASE * rho / C.UPKEEP_QI_BASE
    if ratio <= 0:
        return -1.0
    return ratio ** (1.0 / gap) - 1.0


# ------------------------------------------------------------------ ผลผลิตของสำนัก
def sect_output(members: int, territory_qi: float) -> float:
    """ปราณที่สำนักหนึ่งขุดได้ต่อเดือน — คอบบ์-ดักลาสจากศิษย์คูณอาณาเขต

    อาณาเขตไม่ใช่ field ใหม่ที่ต้องดูแล มันคือผลรวมความหนาแน่นปราณของสถานที่ที่ศิษย์
    ของสำนักยืนอยู่จริง (ดู sim.sect_territory) เลือกนิยามนี้เพราะมันทำให้ "การแผ่
    อิทธิพล" เป็นสิ่งที่วัดได้จากพฤติกรรมของคนในสำนักเอง ไม่ใช่ตัวเลขที่ใครไปตั้ง
    และทำให้สงครามสำนักมีของรางวัลที่ผลิตซ้ำได้ ไม่ใช่แค่เงินปล้นครั้งเดียว
    """
    return PHYS.cobb_douglas(C.SECT_TFP, max(0, int(members)),
                             max(0.0, float(territory_qi)), C.SECT_ALPHA)


def transfer_qi(payer, payee, value: float, grade: int) -> float:
    """จ่ายค่าของด้วยหินวิญญาณ — **ย้ายมือ ไม่ใช่เผาทิ้ง**

    ต่างจาก burn_qi ตรงนี้สำคัญมากและพลาดง่าย: การซื้อขายไม่ทำให้ปราณหายจากโลก
    มันแค่เปลี่ยนเจ้าของ ถ้าใช้ burn_qi ในการจ่ายเงิน บัญชีของโลกจะรั่วทุกครั้งที่มีคน
    ซื้อของ แล้วคลังฟ้าจะไม่มีวันสมดุล — เป็นคนละกลไกกับการเผาหินเพื่อเลี้ยงขั้น
    ซึ่งปราณถูกใช้ไปจริงและควรหายไปจริง

    คืนมูลค่าที่จ่ายได้จริง ผู้รับได้เป็นหินเกรดที่ระบุ (เกรดของแดนที่ซื้อขายกัน)
    """
    paid = burn_qi(payer, value)
    if paid > 0:
        add_stones(payee, grade, mint(paid, grade))
    return paid
