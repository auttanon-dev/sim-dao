# รันตรวจบัคและโลก LLM

เปิดโฟลเดอร์นี้ใน VS Code แล้วกด `Ctrl+Shift+B`:

- `Sim Dao: ตรวจบัคเร็ว` — compile + regression ที่เกี่ยวข้อง + integrity audit
- `Sim Dao: ตรวจบัคเต็มระบบ` — unittest ทั้ง repository + determinism + all realms
- `Sim Dao: ศูนย์ควบคุมผู้มีจิตใจและนิยาย` — เปิดหน้าเว็บตั้งเวลา จำนวนคน เลือกตัวละคร และแต่งนิยาย

ทางลัดบน Desktop ชื่อ **Sim Dao - Real LLM** เปิดศูนย์ควบคุมที่ `http://127.0.0.1:8002`:
กำหนดเวลารันและจำนวนผู้มีจิตใจได้เอง คลิกตัวละครเพื่ออ่านชีวิต แล้วกดแต่งนิยายพร้อมกำหนดจำนวนบท
โลกและนิยายเก็บแยกไว้ที่ `out/minds-compare-6/` และระบบไม่อนุญาตให้สองงานแย่งโมเดลพร้อมกัน

ผลการตรวจอยู่ใน `out/audit/` และผลชั้นจิตใจอยู่ใน `out/minds-compare-6/`

## เหตุผลของ tool selection

ใช้ compiler/tests/determinism หา logic bugก่อน เพราะเร็ว ทำซ้ำได้ และไม่ต้องเดา ส่วน `Compare-Minds`
ใช้ Local LLM กับตัวละคร 9 คนเฉพาะจังหวะที่ต้องคิดจริง ให้ LLM เปลี่ยนเจตนาและเส้นทางประวัติศาสตร์
โดยเอนจินยังเป็นผู้ตัดสินผลลัพธ์ตามกฎโลก
